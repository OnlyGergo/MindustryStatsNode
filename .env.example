# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mindustry_stats
DB_USER=postgres
DB_PASSWORD=your_password

# Server Configuration
PORT=3000
NODE_ENV=development

# Migration flag (set to 'true' to migrate data from JSON to database on startup)
MIGRATE_DATA=false